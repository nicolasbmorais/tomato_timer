import 'package:flutter/material.dart';
import 'package:tomato_timer/core/core.dart';

class TakeShortBreak extends StatelessWidget {
  const TakeShortBreak({super.key});

  @override
  Widget build(BuildContext context) {
    return const TemplateUI(body: Column());
  }
}
