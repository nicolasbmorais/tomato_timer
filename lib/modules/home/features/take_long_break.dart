import 'package:flutter/material.dart';

class TakeLongBreak extends StatelessWidget {
  const TakeLongBreak({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
